import React from "react";

const SectionHeading = ({ title }) => {
  return (
    <h1 className="text-3xl font-bold text-center mb-8 tracking-tight text-primary">
      {title}
    </h1>
  );
};

export default SectionHeading;
